spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: employee-service
  profiles:
    active: dev 
  cloud:
    config:
      uri: http://localhost:8088

        
       
  
    
